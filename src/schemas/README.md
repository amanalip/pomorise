# Runtime schema source boundary

Phase 5 will place Zod schemas for persisted and imported unknown values here. Every schema must protect the local-data trust boundary before application code uses a value.
