# Shared component source boundary

Phase 2 will place accessible project-owned interface primitives here. The minimal Phase 1 product shell remains under `src/app/` until reusable components are approved.
