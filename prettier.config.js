// Export the deterministic formatting choices shared by source, tests, styles, and documentation.
export default {
  // Keep wrapped prose and code readable without creating excessively wide review diffs.
  printWidth: 100,
  // Use trailing commas where modern JavaScript allows them to make later additions cleaner.
  trailingComma: "all",
  // Close the formatter configuration after defining the two intentional project overrides.
};
